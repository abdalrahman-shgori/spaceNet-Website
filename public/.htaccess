<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirect www to non-www and force HTTPS
  RewriteCond %{HTTP_HOST} ^www\.spacecraftiq\.com [NC]
  RewriteRule ^(.*)$ https://spacecraftiq.com/$1 [L,R=301]

  # Force HTTPS if not already
  RewriteCond %{HTTPS} off
  RewriteCond %{HTTP_HOST} ^spacecraftiq\.com [NC]
  RewriteRule ^(.*)$ https://spacecraftiq.com/$1 [L,R=301]

  # Existing rules
  RewriteBase /
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ /index.html [L]
</IfModule>

# Cache headers: ensure HTML is not cached, allow long cache for hashed assets
<IfModule mod_expires.c>
  ExpiresActive On
  # Never cache HTML (so new builds are picked up)
  ExpiresByType text/html "access plus 0 seconds"
  # Cache static assets for 1 year (filenames are content-hashed)
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType text/javascript "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
  ExpiresByType application/x-javascript "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType image/x-icon "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType font/ttf "access plus 1 year"
  ExpiresByType font/otf "access plus 1 year"
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

<IfModule mod_headers.c>
  # Prevent caching of HTML
  <FilesMatch "\.(html)$">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
  </FilesMatch>
  # Strong caching for versioned static assets
  <FilesMatch "\.(?:js|css|png|jpg|jpeg|gif|webp|ico|svg|ttf|otf|eot|woff|woff2)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
</IfModule>