const router = require('react-router'); // Import your routes
const Sitemap = require('react-router-sitemap').default;

const routes = [
  { path: '/' },
  { path: '/software' },
  { path: '/design-branding' },
  { path: '/academics' },
  { path: '/core-it' },
  { path: '/blogs' },
];

new Sitemap(router)
  .build('https://spacenetiq.com/')
  .save('./public/sitemap.xml');